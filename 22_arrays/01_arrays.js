// intro to arrays
// Array is reference type
// collection of elements
// Order collection of items
// how to create arrays
// ordered collection of itmes

// all reference types are objects 

let fruits = ["apple", "mango", "grapes"];
 console.log(fruits[0]);     //for access index we use [] brackets

let num = [1, 2, 3, 4];
console.log(num);

// in array we can store different types of data

let mixed = [1, 2, 3, 4, "string", null, undefined];
console.log(mixed);
// for access index we use [] brackets

// array indexing
